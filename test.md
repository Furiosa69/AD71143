# TEST
## 时钟频率测试
利用示波器来测量时钟频率clk_100m/clk_125m/gate_clk是否正常 [yes]
验证AFE控制模块sync/aclk [yes]


## SPI 测试
上电后发现 REF_TFT 的电压为1.9V，应该是2.6V，REF_INT是-1.9V，经过排查，REF_ADC是4.0724，AVDD2是2.5V，AVDD5B是5.03V，DVDD5是5.03V，DVDD2是2.49V

用ILA检查SPI配置寄存器是否成功:修改了SPI的频率后能在ILA查看波形
SCK在查看波形后计算确实是25Mhz，SDI有数据，CS有完整周期，但是SDO还是全零
使用testbench来进行测试，SDO也为5.0
不是代码逻辑的问题，cfg_state能正常到达CFG_DONE,以及SPI_TEST_Top独立测试是正常跑的
不是SPI帧格式错误或者频率有错误，用ILA校验过
不是IOSTANDARD不匹配，AD手册上要求的就是2.5V
供电没有异常，在负载情况下进行测试均在范围内
不是RESET异常，用ILA确认了
不是SDO走线断掉，万用表蜂鸣器检测过
只有可能是AD芯片的SPI接口有问题，怀疑是因为上电顺序导致烧掉了

有两个解决方案，但是都需要重新购买AD芯片，
1.维持原来的电压参数（AVDDI=3.9,AVSSI=1.9），但是就必须要求严格的上电顺序，实现起来可能有点困难
2.根据manual的说法，如果让AVSSI接地，那么其它电源就会天然在AVSSI之前供应，这样的话，相关的电压要求就需要修改，AVDDI就需要改为2.5V,AVSSI则为0

考虑是不是AD的电源还没稳定就把SPI写入了,不是这个原因，也不是SPI写间距太小了的原因。

现在的情况是SDO只进行了一次电平翻转，并且REF_TFT的实测电压为0,不是1.0,推测为写寄存器操作没有完成。


## 网口测试
现在抓捕不到udp.port == 1234的网络
并且FPGA侧的网口不亮，现在是PHY层没有连接
PHY芯片型号是YT8531C,原因在没有绑定引脚
没有绑定引脚是其中一个原因,以及MDIO没有配置好？
现在MDIO配置好了，但是用wireshark依旧抓取不到东西
网口就是enp0s13f0u2u1
