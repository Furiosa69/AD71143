# 图像滚动问题 - 最终解决方案

## 问题根源

图像滚动的根本原因是**帧边界不对齐**：

### 实际情况
- FPGA 发送的测试图像是静态的（每帧相同的渐变图案）
- 每行像素值 = 行号 × 121
  - 行 0: 像素值 = 0
  - 行 1: 像素值 = 121
  - 行 470: 像素值 = 56870
  - 行 540: 像素值 = 65340

### 调试发现
通过 `debug_frame_alignment.py` 发现：
```
接收到的第一行推断行号: 517 (期望=0)
接收到的最后行推断行号: 375 (期望=499)
```

**软件每 500 行切分一次，但起始点不是硬件的行号 0**，导致：
- 第1次显示：从硬件行517开始
- 第2次显示：从硬件行xxx开始（每次不同）
- 看起来图像在"滚动"，实际上是每次显示的起始行不同

## 解决方案

### 方案1: 魔数检测（理想但失败）
- FPGA 在每帧第一个包的最高16位发送 0xAA55
- 软件检测到 0xAA55 时触发新帧
- **问题**: 实际测试中未检测到魔数，可能原因：
  - 字节序问题
  - FIFO 深度不足时魔数包被丢弃
  - 或者魔数未正确发送

### 方案2: 行号跳变检测（最终方案）✓

**原理**:
- 每个包的像素值可以推断出行号：`inferred_line = pixel_value / 121`
- 正常情况下行号递增：0→1→2→...→540
- 帧边界处行号大幅下降：470→0（跨帧）

**实现**:
```python
# 推断当前包的行号
avg_value = np.mean(samples)
inferred_line = int(avg_value / 121)

# 检测行号大幅下降（> 400）
if self.last_inferred_line > 0 and inferred_line < self.last_inferred_line - 400:
    # 检测到新帧开始
    # 保存当前帧
    # 重置缓冲区
    # 返回完整帧
```

**文件**: `host_pc/data_parser.py`

**优点**:
- ✓ 不依赖硬件魔数
- ✓ 可以用于测试渐变图像
- ✓ 可以推广到真实传感器数据（只要行间有亮度差异）

**限制**:
- 仅适用于行间亮度有变化的图像
- 如果图像是纯色（所有行相同），无法检测帧边界

## 更通用的解决方案（推荐用于生产）

### 硬件修改：增强帧起始标记

#### 选项A: 在 UDP payload 最前面插入帧号
修改 `rgmii_bridge.v`：
```verilog
// 在每个 UDP payload 的最前 2 字节插入帧计数器
reg [15:0] frame_counter;

always @(posedge clk_125m) begin
    if (frame_start_detected) begin
        frame_counter <= frame_counter + 1;
    end
end

// 封装时：payload = {frame_counter[15:0], burst_data[239:0]}
```

软件端检测帧号跳变即可识别新帧。

#### 选项B: 使用独立的帧同步包
- 每帧开始前发送一个特殊的同步包（不同的 UDP 端口或特殊内容）
- 软件检测到同步包后重置缓冲区

### 软件修改：更鲁棒的边界检测

#### 选项C: 多重检测策略
```python
def detect_frame_boundary(self, samples, time_gap):
    # 策略1: 时间间隔 > 10ms
    if time_gap > 0.01:
        return True
    
    # 策略2: 行号大幅下降
    inferred_line = self.infer_line_number(samples)
    if inferred_line < self.last_line - 400:
        return True
    
    # 策略3: 包计数器达到阈值
    if self.packet_count >= EXPECTED_PACKETS_PER_FRAME:
        return True
    
    return False
```

## 当前状态

### 已修复 ✓
- FIFO 深度从 16 增加到 1024
- 实现基于行号跳变的帧同步
- 双缓冲机制（帧完成时缓存，下次边界时返回）

### 测试方法
```bash
cd host_pc

# 1. 检查帧对齐
python debug_frame_alignment.py

# 2. 运行主程序
python main.py
```

### 预期效果
- ✓ 图像不再滚动
- ✓ 每帧从行号 0 开始
- ✓ 显示稳定的渐变图案（上黑下白）

## 后续工作

### 短期
1. 测试当前方案是否解决滚动问题
2. 验证真实传感器数据是否也能正确同步

### 中期
3. 在 FPGA 中实现更可靠的帧起始标记（帧号或同步包）
4. 优化软件端的帧同步逻辑

### 长期
5. 添加帧丢失检测和恢复机制
6. 实现自适应帧同步（自动学习帧结构）

## 相关文件

- `host_pc/data_parser.py` - 帧同步核心逻辑
- `host_pc/debug_frame_alignment.py` - 帧对齐诊断工具
- `host_pc/find_nonzero_packets.py` - 数据包内容检查
- `图像滚动原因分析.md` - 问题分析文档
- `帧同步问题分析.md` - 之前的分析（FIFO 深度问题）
- `FIFO深度修复指南.md` - FIFO 扩容操作指南
