# AD71143 帧同步问题分析与解决方案

## 问题描述
图像从上到下滚动变化，无法稳定显示。

## 根本原因

### 1. FIFO 深度不足（主要问题）
- **位置**: `board/project_1/project_1.srcs/sources_1/ip/fifo_generator_0/fifo_generator_0.xci`
- **当前配置**: `Input_Depth = 16`
- **问题**: FIFO 只能缓冲 16 个 burst（32μs 数据），远小于一帧所需（34624 个 burst）
- **后果**: FIFO 满时丢弃数据，导致只能接收约 309/541 行（57%）

**数据速率分析**:
```
生成速度: 100MHz, 每 200 周期发送 1 个 burst = 500k bursts/s
发送速度: 125MHz, 每个包 78 字节需要 156 周期 ≈ 800k bursts/s 理论最大值
实际瓶颈: 以太网物理层限制 + CRC 计算开销
结果: 生成速度 > 发送速度 → FIFO 溢出
```

### 2. 软件帧同步缺失（已修复）
- **原问题**: UDP 接收缓冲压缩了帧间隔，时间戳不稳定
- **解决方案**: 使用行数阈值（300 行）触发帧边界，不依赖时间间隔

## 解决方案

### 短期方案（已实施）✓
修改软件帧同步逻辑，基于固定行数触发新帧：

**文件**: `host_pc/data_parser.py`
```python
# 在 FrameAssembler.__init__() 中
self.lines_per_frame_threshold = 300  # 匹配实际接收行数

# 在 process_packet() 中
if self.current_line >= self.lines_per_frame_threshold:
    # 触发新帧
    frame_data = self.frame_buffer.copy()
    self.frame_count += 1
    self.reset_frame()
    return True, frame_data
```

**效果**:
- ✓ 图像不再滚动
- ✓ 每帧在相同位置刷新
- ✗ 只显示 300/541 行（图像不完整）

### 长期方案（推荐）
增加 FIFO 深度以接收完整帧：

#### 步骤 1: 修改 FIFO IP 核配置
1. 在 Vivado 中打开项目：`board/project_1/project_1.xpr`
2. IP Catalog → 找到 `fifo_generator_0`
3. 修改配置：
   - `Input_Depth`: 16 → **512** 或 **1024**
   - `Output_Depth`: 自动匹配
4. 重新生成 IP 核

**推荐深度计算**:
```
每帧 34624 个 burst
安全 FIFO 深度 >= 1024 个 burst
内存占用 = 1024 × 256 bits = 32 KB
```

#### 步骤 2: 修改软件帧阈值
**文件**: `host_pc/data_parser.py`
```python
# 恢复为期望的完整帧
self.lines_per_frame_threshold = 500  # 接近 541 行
```

#### 步骤 3: 重新综合与实现
```bash
# 在 Vivado TCL console 中
reset_run synth_1
launch_runs impl_1 -to_step write_bitstream -jobs 8
wait_on_run impl_1
```

#### 步骤 4: 下载新 bitstream
```
board/project_1/Result/top.bit
```

## 验证测试

### 测试脚本
```bash
cd host_pc
python test_line_distribution.py  # 检查每帧行数
python main.py                     # 查看图像是否完整且稳定
```

### 期望结果
- 每帧接收 **541 行**（34624 个 burst）
- 图像不滚动，完整显示
- 帧率稳定（约 20 fps，基于 50ms 帧间隔）

## 当前状态

### 已完成 ✓
- [x] 诊断问题根因：FIFO 深度不足
- [x] 实现软件帧同步（基于行数阈值）
- [x] 消除图像滚动现象
- [x] 验证帧同步工作正常

### 待完成
- [ ] 增加 FIFO 深度到 512 或 1024
- [ ] 重新综合生成 bitstream
- [ ] 验证接收完整 541 行
- [ ] 更新软件阈值为 500 行

## 相关文件

### FPGA 硬件
- `board/project_1/project_1.srcs/sources_1/imports/src/top.v`
  - 测试图像生成器（line 880-994）
  - 帧间隔状态机（50ms gap）
  
- `board/project_1/project_1.srcs/sources_1/imports/src/rgmii_bridge.v`
  - FIFO 写入逻辑（line 93-94）
  - 数据丢弃条件：`!fifo_full`
  
- `board/project_1/project_1.srcs/sources_1/ip/fifo_generator_0/`
  - FIFO IP 核配置（需要修改深度）

### 软件
- `host_pc/data_parser.py`
  - FrameAssembler 类（帧组装与同步）
  - 当前配置：300 行阈值
  
- `host_pc/main.py`
  - GUI 主程序
  - 调用 data_parser.py 进行帧组装

### 测试工具
- `host_pc/test_frame_gap.py` - 测量帧间隔和包数
- `host_pc/test_line_distribution.py` - 统计每帧行数分布

## 附加说明

### 为什么软件无法检测硬件的 50ms 帧间隔？
UDP 接收缓冲（OS kernel buffer）会累积数据包，然后批量传递给应用层。这导致：
- 硬件发送的 50ms 大间隔被压缩成多个小间隔（3-6ms）
- 间隔随机出现在帧中间，不在帧边界
- 基于时间的帧同步不可靠

因此改用**行数阈值**方法，稳定可靠。

### 为什么是 309 行而不是其他值？
```
FIFO 深度 = 16 burst
发送一个包 ≈ 1.25μs
生成间隔 = 2μs/burst
累积数据率差异导致 FIFO 在发送约 19800 个 burst（309 行）后满
之后每个新 burst 都被丢弃，直到进入 50ms 帧间隔
```

### 真实传感器数据会工作吗？
会的。当前方案基于：
- **行数计数**：不依赖图像内容
- **固定阈值**：每 300 行触发新帧

对测试渐变和真实传感器数据都适用。

升级 FIFO 后，可以接收完整 541 行，适用于所有数据源。
