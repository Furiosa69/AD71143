# 图像滚动问题深度分析

## 测试图像是静态的 ✓

FPGA 生成的测试图像每帧都相同：
```verilog
pixel_value = test_line_cnt * 121
```

- 第 0 行：所有像素 = 0（黑色）
- 第 1 行：所有像素 = 121
- 第 2 行：所有像素 = 242
- ...
- 第 540 行：所有像素 = 65340（白色）

**每帧内容完全相同，不随时间变化。**

## 滚动的可能原因

### 原因 1: 帧边界不对齐（最可能）

**问题**: 软件认为的"第 0 行"和硬件发送的"第 0 行"不同步

**示例**:
```
硬件发送（每帧 500 行）:
帧1: 行0, 行1, 行2, ..., 行499
帧2: 行0, 行1, 行2, ..., 行499

软件接收（如果不对齐）:
第1次显示: 行250, 行251, ..., 行499, 行0, 行1, ..., 行249
第2次显示: 行300, 行301, ..., 行499, 行0, 行1, ..., 行299
第3次显示: 行350, 行351, ..., 行499, 行0, 行1, ..., 行349
```

**现象**: 看起来图像在"滚动"，实际上是每次显示的起始行不同

**原因**: 
- 软件的 500 行阈值触发点随机
- 没有等待真正的帧起始信号（行号=0）
- 每次拼接的 500 行可能跨越了两帧

### 原因 2: 数据包乱序或丢包

如果网络层数据包乱序：
```
正常: 行0→行1→行2→...
乱序: 行2→行0→行1→...
```

显示时会出现错乱，看起来像滚动。

### 原因 3: 帧缓冲区未清零

如果新帧覆盖旧帧时：
```
旧帧残留: [行450-499有数据, 行0-449为0]
新帧写入: [行0-499写入新数据]
```

如果新帧只写了部分，残留数据会造成视觉上的"滚动"。

## 验证方法

让我写一个调试工具来检查实际接收到的数据：
