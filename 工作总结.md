# AD71143 图像接收系统 - 工作总结

## 完成的任务

### 1. 图像滚动问题修复 ✓

**问题根因**：
- 帧边界不对齐：软件每 500 行切分，但起始点不是硬件的行号 0
- 导致每次显示的起始行号不同，看起来像"滚动"

**解决方案**：
- 测试模式：基于行号跳变检测（行号从大→小时触发新帧）
- 真实数据模式：基于固定包数计数（19500 包/帧）

**结果**：
- ✓ 图像不再滚动
- ✓ 帧边界对齐
- ✓ 显示稳定

### 2. FIFO 深度扩容

**问题**：
- 原 FIFO 深度只有 16，导致数据溢出
- 每帧只能接收约 306/541 行（57%）

**修改**：
- 修改 `fifo_generator_0.xci`：深度从 16 → 1024
- 更新相关参数（计数器位宽、阈值等）

**状态**：
- ⚠️ 配置已修改，但需要重新生成 bitstream
- ⚠️ 当前 bitstream 依然只能发送约 306 行

### 3. 测试模式 → 真实 AFE 数据切换

**硬件修改**：
- `top.v`：`DATA_TEST_MODE = 0`（切换到真实 AFE 数据）

**软件适配**：
- 帧同步改为基于包数计数（不依赖图像内容）
- 适配 306 行帧大小

**状态**：
- ✓ 代码已修改
- ⚠️ 需要重新生成 bitstream
- ⚠️ 当前 LVDS 数据接收有问题（shift_hi/shift_lo 无数据）

## 当前系统状态

### 硬件（FPGA）
- **Bitstream 版本**：2026-07-28 17:53
- **数据源**：测试图像模式（bitstream 未更新）
- **FIFO 深度**：16（配置已改为 1024，但 bitstream 未更新）
- **接收行数**：约 306 行
- **LVDS 状态**：未测试（需要切换到真实数据后验证）

### 软件
- **帧同步方式**：基于固定包数（19500 包/帧）
- **帧大小配置**：306 行 × 512 列
- **帧率**：约 20 fps
- **滚动问题**：✓ 已修复

## 待完成的工作

### 立即需要（切换到真实 AFE 数据）

1. **重新生成 bitstream**
   ```tcl
   source D:/project/AD71143/board/switch_to_afe_data.tcl
   ```
   - 应用 `DATA_TEST_MODE = 0`
   - 应用 FIFO 深度 1024（如果之前的 IP 更新成功）

2. **下载新 bitstream 到 FPGA**

3. **使用 ILA 诊断 LVDS 数据接收**
   - 参考：`AFE_LVDS数据接收诊断清单.md`
   - 检查：`spi_cfg_done`, `frame_start`, `sync_int`, `shift_hi/lo`

4. **验证真实 AFE 图像显示**
   ```bash
   cd host_pc
   python main.py
   ```

### 短期优化

5. **验证 FIFO 深度是否生效**
   - 运行 `test_line_distribution.py`
   - 检查是否能接收完整 541 行

6. **如果依然只有 306 行**
   - 检查 Vivado 中 FIFO IP 核配置
   - 考虑进一步增加 FIFO 深度到 2048

### 中期改进

7. **添加硬件帧计数器**（推荐）
   - 在 `rgmii_bridge.v` 中每个包插入帧号
   - 软件端检测帧号跳变触发新帧
   - 更可靠，不受丢包影响

8. **优化 RGMII 发送速率**
   - 减少帧封装开销
   - 提高网络吞吐量

## 关键文件清单

### 硬件（FPGA）
- `board/project_1/project_1.srcs/sources_1/imports/src/top.v` - 顶层，数据源选择
- `board/project_1/project_1.srcs/sources_1/ip/fifo_generator_0/fifo_generator_0.xci` - FIFO 配置
- `board/project_1/project_1.srcs/sources_1/imports/src/rgmii_bridge.v` - 以太网发送
- `board/project_1/project_1.srcs/sources_1/imports/src/ad71143_ctrl.v` - AFE 控制
- `board/project_1/project_1.srcs/sources_1/imports/src/ad71143_data_rx_dual.v` - LVDS 接收

### 软件
- `host_pc/data_parser.py` - 数据解析和帧同步
- `host_pc/main.py` - GUI 主程序

### 脚本
- `board/switch_to_afe_data.tcl` - 切换到真实数据并生成 bitstream
- `board/rebuild_bitstream.tcl` - 重新生成 bitstream（通用）

### 文档
- `图像滚动问题-最终解决方案.md` - 滚动问题分析和解决方案
- `切换到真实AFE数据-操作指南.md` - 切换步骤
- `AFE_LVDS数据接收诊断清单.md` - LVDS 问题诊断
- `FIFO深度修复指南.md` - FIFO 扩容操作
- `帧同步问题分析.md` - 早期问题分析

### 调试工具
- `host_pc/debug_frame_alignment.py` - 帧对齐诊断
- `host_pc/monitor_line_jumps.py` - 行号跳变监控
- `host_pc/test_line_distribution.py` - 每帧行数统计
- `host_pc/test_frame_gap.py` - 帧间隔测试
- `host_pc/find_nonzero_packets.py` - 查找非零数据包
- `host_pc/inspect_packets.py` - 数据包内容检查

## 技术要点

### 帧同步方法对比

| 方法 | 适用场景 | 优点 | 缺点 |
|------|---------|------|------|
| 魔数检测 | 理想方案 | 精确、可靠 | 硬件未实现或未检测到 |
| 行号跳变 | 测试渐变图像 | 精确对齐 | 只适用于特定图像 |
| 固定包数 | 通用（当前） | 简单、通用 | 对丢包敏感 |
| 帧计数器 | 生产推荐 | 可靠、抗丢包 | 需要硬件修改 |

### FIFO 容量计算

```
当前: 16 × 256 bits = 512 bytes = 16 bursts
     缓冲时间 = 16 × 2μs = 32μs

修改后: 1024 × 256 bits = 32 KB = 1024 bursts
       缓冲时间 = 1024 × 2μs = 2048μs ≈ 2ms

一帧数据: 541 行 × 64 burst/行 = 34624 bursts
         持续时间 ≈ 69ms

FIFO 可缓冲约 3% 的帧数据（足够平滑速率差异）
```

### 数据速率分析

```
生成速度: 100MHz, 每 200 周期 = 500k bursts/s
发送速度: 125MHz, 理论最大 ≈ 800k bursts/s
实际瓶颈: 以太网封装 + CRC 计算开销

当前接收: 约 306 行/帧 = 19584 bursts/帧
期望接收: 541 行/帧 = 34624 bursts/帧
```

## 已知问题和限制

1. **FIFO 深度配置未验证**
   - 配置文件已修改，但需要重新生成 bitstream 验证

2. **帧同步对丢包敏感**
   - 当前方法基于固定包数
   - 丢包会导致帧边界漂移
   - 建议：添加硬件帧计数器

3. **只接收 306/541 行**
   - FIFO 深度不足导致数据溢出
   - 需要验证 1024 深度是否生效
   - 可能需要进一步优化

4. **LVDS 数据接收未验证**
   - 切换到真实数据后需要诊断
   - 可能的问题：SPI 配置、时序、物理连接

## 联系和支持

如有问题，请检查：
1. ILA 波形（硬件调试）
2. `frame_debug.log`（软件调试日志）
3. 相关文档和诊断工具

祝调试顺利！
